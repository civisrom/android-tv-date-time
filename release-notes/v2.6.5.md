## Android TV Time Fixer 2.6.5 — предварительный выпуск

**Приложение для Android работает в тестовом режиме.**

- Добавлен терминал ADB с историей команд, справкой и предупреждением о рисках.
- Добавлены передача файлов и установка APK и split APK на подключённое устройство. Исправлена обработка имён файлов; сохранённые копии можно удалять из списка.
- Улучшено беспроводное сопряжение и подключение после него. Подключение и сопряжение теперь можно отменить.
- Индикаторы перенесены к соответствующим разделам, статус подключённого найденного устройства выделен жирным шрифтом.
- Отказ прошивки в праве изменить NTP-сервер теперь объясняется понятным сообщением.


---

## Android TV Time Fixer 2.6.5 — prerelease

**The Android app operates in test mode.**

- Added an ADB terminal with command history, reference examples, and a risk warning.
- Added file transfers and APK and split APK installation on connected devices. Fixed filename handling; saved copies can be deleted from the list.
- Improved wireless pairing and the connection that follows it. Connection and pairing attempts can now be cancelled.
- Progress indicators appear beside their corresponding sections, and connected discovered devices have a bold status label.
- A clear message now explains when the target firmware denies permission to change the NTP server.


APK: GPL-3.0 (LICENSE-GPL-3.0.txt); исходный код проекта и desktop: Apache-2.0.

APK: GPL-3.0 (LICENSE-GPL-3.0.txt); project sources and desktop: Apache-2.0.


---

## Обновление выпуска 2.6.5

Предыдущее описание выше сохранено. Android-приложение продолжает работать в тестовом режиме.

- Добавлены исходные снимки настроек времени и профили устройств с подтверждением, проверкой идентичности и результатов записи. Снимки доступны после перезапуска; ошибки и частичное восстановление показываются явно.
- Добавлены резервные NTP для Android 14+, поддержка IPv6, наблюдение за часами до 10 минут и обезличенный экспорт диагностики. Сохранение настройки, совпадение часов и подтверждение источника времени показываются раздельно.
- Десктопная версия при запуске проверяет сеть, HTTPS, NTP и порт последнего устройства. Подсказки учитывают блокировку интернета и локальных подключений фаерволом, VPN, прокси и изоляцию Wi-Fi. Работа без интернета остаётся доступной.
- Общий каталог расширен на 38 адресов, проверенных настоящими NTP-запросами: теперь 91 альтернативный адрес и 77 стран. Доступность конкретного сервера зависит от сети пользователя.
- В Android исправлены навигация пультом в настройках времени, элементы терминала на коротких экранах с крупным шрифтом и устаревшие адреса обнаруженных устройств. Справочник расширен до 154 примеров в 10 категориях; обновлены криптографические зависимости.
- Исправлены выбор NTP, импорт путей с `~`, очистка служебного ADB и таймауты команд. Расширены проверки отказов сети, прав доступа, отмены, восстановления и интерфейса.
- Добавлена отдельная сборка macOS для Intel; доступны Windows, Linux, macOS Apple Silicon и Intel. Актуализированы оба README.

## Release 2.6.5 update

The previous description above is preserved. The Android app continues to operate in test mode.

- Added original time-setting snapshots and device profiles with confirmation, identity checks and readback verification. Snapshots survive restarts; failures and partial restorations are reported explicitly.
- Added fallback NTP for Android 14+, IPv6 support, clock monitoring for up to 10 minutes and anonymized diagnostic exports. Stored settings, clock comparison and time-source confirmation are presented separately.
- Desktop startup checks the network, HTTPS, NTP and the last device's port. Guidance covers Internet and LAN firewall restrictions, VPNs, proxies and Wi-Fi isolation. Offline use remains available.
- Added 38 addresses verified with real NTP requests to the shared catalog: 91 alternative addresses and 77 countries. Individual server reachability depends on the user's network.
- Fixed remote-control navigation in Android time settings, terminal controls on short screens with large fonts and stale discovered-device addresses. Expanded the reference to 154 examples in 10 categories and updated cryptographic dependencies.
- Fixed NTP selection, importing paths with `~`, application ADB cleanup and command deadlines. Expanded network, permission, cancellation, restoration and interface checks.
- Added a separate macOS Intel build; Windows, Linux, macOS Apple Silicon and Intel builds are available. Updated both READMEs.
